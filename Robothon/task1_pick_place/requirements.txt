# Task 1 – Object Pick and Place
# Physical AI Hackathon 2026
# Python >= 3.10 required

# ── Core simulation ──────────────────────────────────────────────────────────
mujoco>=3.1.0
numpy>=1.24.0

# ── Gymnasium (LeRobot-compatible RL environment) ────────────────────────────
gymnasium>=0.29.1

# ── Deep learning ────────────────────────────────────────────────────────────
torch>=2.1.0
torchvision>=0.16.0

# ── Data / serialisation ─────────────────────────────────────────────────────
huggingface_hub>=0.20.0
datasets>=2.14.0

# ── Video recording (optional) ───────────────────────────────────────────────
opencv-python>=4.8.0

# ── Visualisation ────────────────────────────────────────────────────────────
matplotlib>=3.7.0

# ── LeRobot (optional – enables native ACT/Diffusion training) ───────────────
# Uncomment the line below to install the full LeRobot library:
# lerobot @ git+https://github.com/huggingface/lerobot.git

# ── Config management (needed for LeRobot native mode) ───────────────────────
omegaconf>=2.3.0
hydra-core>=1.3.0
